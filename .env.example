NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secret key from your Paystack dashboard (Settings > API Keys & Webhooks).
# Never prefix this with NEXT_PUBLIC_ — it must stay server-side only.
PAYSTACK_SECRET_KEY=
